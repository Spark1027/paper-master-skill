# 可复用调用请求

以下文字依据 2026-09-13 的任务范围整理，供读者复用，不是原始对话逐字导出。

```text
使用 $paper-master 阅读 Zhang 等的正式发表版论文：
In vivo whole-cortex marker of excitation-inhibition ratio indexes cortical maturation and cognitive ability in youth
DOI: 10.1073/pnas.2318641121

使用模块 1（七节通用总结）+2+3+4。
说明模型候选标记的定义、关键参数和证据边界。
相关文献只做有限检索，优先比较不同 E/I 指标的定义与验证；不要写成系统综述。
不运行仿真、不安装依赖、不使用付费 API；本案例不使用 PDF。
若网页不能读取，说明当次限制，不把本文历史报告当作已经亲自读到的原文。
提供一份 Markdown 报告，使用 GitHub 的数学定界符；保留原始输入。
```

历史执行采用所提供的正式发表版 XML 与可读文本。复用请求时如需提供本地材料，请自行从原文合法获取并明确版本；本仓库不捆绑论文全文。
