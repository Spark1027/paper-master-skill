# 调用请求

以下请求对应 [输入摘录](input/model-excerpt.md)。请将摘录作为附件，或将其内容粘贴在请求后。

```text
使用 $paper-master，只做模块 1 的通用总结。
仅阅读我提供的 model-excerpt.md，不联网、不引用外部文献、不运行仿真。
保留输入中已有的核心公式、参数、单位和结果来源，明确这是合成教学材料。
把报告保存为一个新的 Markdown 文件，不修改输入；为 GitHub 阅读使用 $...$ 和 $$...$$ 数学定界符。
```

默认行为示例见 [default-md/report.md](default-md/report.md)。该文件是整理者依据输入编写的示范答案，不冒称独立模型测试结果。实际输出允许措辞不同；需要核对的是模块选择、来源边界、数学含义和未执行事项。
